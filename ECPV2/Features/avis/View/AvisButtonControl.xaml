<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="ECPV2.Features.avis.View.AvisButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <StackPanel Spacing="10" Margin="10" MinWidth="120">
        <Button Content="Nouveau"
                Command="{Binding CommandAvisNew,Mode=TwoWay}"/>

        <Button Content="Éditer"
                Command="{Binding CommandAvisEdit,Mode=TwoWay}"/>

        <Button Content="Enregistrer"
                Command="{Binding CommandAvisSave,Mode=TwoWay}"/>

        <Button Content="Supprimer"
                Command="{Binding CommandAvisDelete,Mode=TwoWay}"/>

        <Button Content="Annuler"
                Command="{Binding CommandAvisCancel,Mode=TwoWay}"/>
    </StackPanel>
</UserControl>
