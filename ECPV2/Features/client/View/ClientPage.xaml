<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ECPV2.Features.client.View.ClientPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ECPV2.Features.client.View"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barre de recherche -->
        <local:ClientControl Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="10"/>

        <!-- Liste des clients -->
        <local:ClientListControl Grid.Row="1" Grid.Column="0" Margin="10"/>

        <!-- Fiche détaillée du client sélectionné -->
        <local:ClientEditControl Grid.Row="1" Grid.Column="1" 
                                 DataContext="{Binding ClientSelected, Mode=TwoWay}" Margin="10"/>

        <!-- Boutons CRUD -->
        <local:ClientButtonControl Grid.Row="1" Grid.Column="2" 
                                   DataContext="{Binding}" Margin="10"/>
    </Grid>
</Page>